import { Typography } from "@mui/material";

export default function CheckOutPage(params: any) {
    return (
        <Typography variant="h2">only log in user be able to see this CheckOut</Typography>
    )
}